import React, { Component } from "react";
import {Footer, FooterSection, FooterLinkList, FooterDropDownSection, Header, Navigation} from "react-mdl";
import {Link} from "react-router-dom";

class Payment extends Component {
    render() {
        return (
            <div>

                <h1>Payment Page</h1>

            </div>

        );
    }
}

export default Payment;
